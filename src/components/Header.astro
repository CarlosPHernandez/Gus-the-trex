<header class="header">
  <div class="container header__inner">
    <div class="header__top">
      <div class="header__brand">
        <p class="eyebrow header__eyebrow">Public stewardship campaign</p>
        <h1 class="display header__title">Gus</h1>
      </div>
      <p class="header__date">
        <span class="header__date-label">Auction</span>
        <span class="header__date-value">14 Jul 2026</span>
      </p>
    </div>
    <nav class="header__nav" aria-label="Page sections">
      <a href="#how-it-works">How it works</a>
      <a href="#coalition">Coalition</a>
      <a href="#faq">FAQ</a>
    </nav>
  </div>
</header>

<style>
  .header {
    border-bottom: 1px solid color-mix(in srgb, var(--ink) 18%, transparent);
    padding: 1.25rem 0;
    background: var(--bone);
  }

  .header__inner {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .header__top {
    display: flex;
    align-items: flex-end;
    justify-content: space-between;
    gap: 1rem;
  }

  .header__brand {
    min-width: 0;
  }

  .header__eyebrow {
    margin: 0;
  }

  .header__title {
    margin: 0.15rem 0 0;
    font-size: clamp(2rem, 5vw, 2.75rem);
    line-height: 1.05;
  }

  .header__date {
    margin: 0;
    flex-shrink: 0;
    display: flex;
    flex-direction: column;
    align-items: flex-end;
    gap: 0.1rem;
    text-align: right;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--clay);
    font-size: 0.72rem;
    line-height: 1.25;
  }

  .header__date-value {
    font-size: 0.8rem;
    color: var(--ink);
    letter-spacing: 0.04em;
  }

  .header__nav {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.5rem 0.65rem;
  }

  .header__nav a {
    text-decoration: none;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: var(--forest);
    font-size: 0.8rem;
    line-height: 1.2;
    padding: 0.45rem 0.15rem;
  }

  .header__nav a:hover {
    text-decoration: underline;
  }

  @media (min-width: 768px) {
    .header__inner {
      flex-direction: row;
      align-items: flex-end;
      justify-content: space-between;
      gap: 1.5rem;
    }

    .header__top {
      flex: 1;
      min-width: 0;
      align-items: flex-end;
    }

    .header__date {
      flex-direction: row;
      align-items: baseline;
      gap: 0.35rem;
      font-size: 0.75rem;
    }

    .header__date-value {
      font-size: inherit;
      color: var(--clay);
    }

    .header__nav {
      flex-shrink: 0;
      justify-content: flex-end;
      gap: 0.85rem 1.1rem;
    }

    .header__nav a {
      padding: 0;
    }
  }

  @media (max-width: 767px) {
    .header {
      position: sticky;
      top: 0;
      z-index: 100;
      padding: max(0.75rem, env(safe-area-inset-top)) 0 0.65rem;
      background: color-mix(in srgb, var(--bone) 94%, transparent);
      backdrop-filter: blur(10px);
      -webkit-backdrop-filter: blur(10px);
    }

    .header__inner {
      gap: 0.65rem;
    }

    .header__top {
      align-items: center;
    }

    .header__eyebrow {
      font-size: 0.62rem;
      letter-spacing: 0.14em;
      line-height: 1.35;
    }

    .header__title {
      font-size: clamp(1.65rem, 8vw, 2.1rem);
    }

    .header__date {
      padding: 0.45rem 0.6rem;
      border: 1px solid color-mix(in srgb, var(--clay) 45%, transparent);
      border-radius: var(--radius);
      background: color-mix(in srgb, var(--parchment) 70%, transparent);
      font-size: 0.58rem;
    }

    .header__date-value {
      font-size: 0.72rem;
      font-weight: 600;
      color: var(--forest);
    }

    .header__nav {
      flex-wrap: nowrap;
      gap: 0.45rem;
      margin-inline: -0.15rem;
      padding: 0.15rem 0.15rem 0.2rem;
      overflow-x: auto;
      -webkit-overflow-scrolling: touch;
      scrollbar-width: none;
      mask-image: linear-gradient(
        to right,
        transparent,
        #000 0.5rem,
        #000 calc(100% - 0.5rem),
        transparent
      );
    }

    .header__nav::-webkit-scrollbar {
      display: none;
    }

    .header__nav a {
      flex-shrink: 0;
      min-height: 2.75rem;
      padding: 0.55rem 0.9rem;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      font-size: 0.7rem;
      border-radius: var(--radius);
      border: 1px solid color-mix(in srgb, var(--ink) 14%, transparent);
      background: var(--parchment);
    }

    .header__nav a:hover,
    .header__nav a:active {
      text-decoration: none;
      background: color-mix(in srgb, var(--parchment) 60%, var(--bone));
    }
  }
</style>
